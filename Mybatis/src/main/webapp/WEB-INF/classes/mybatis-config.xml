<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
  PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
  
  <!-- settings : mybaits 구동시 선언할 설정들을 작성하는 영역  -->
  <settings>
  <!-- 만약 null로 데이터가 전달되었다면 빈칸이 아닌 null로 인식해라 -->
  	<setting name="jdbcTypeForNull" value="NULL"/>
  </settings> 
  
  <!-- typeAliases : VO/DTO 클래스들의 풀 클래스명을 단순한 클래스명(별칭)으로 사용하기 위한 별칭등록을 하는 영역 -->
  <typeAliases>
  		<typeAlias type="com.kh.mybatis.member.model.vo.Member" alias="Member"/>
  		<typeAlias type="com.kh.mybatis.board.model.vo.Board" alias="Board"/>
  		<typeAlias type="com.kh.mybatis.board.model.vo.Reply" alias="Reply"/>
  </typeAliases>
   
  <!-- environments : mybatis구동시 연동할 db정보를 등록하는 영역 -->
  <environments default="development">
  <environment id="development">
    
    
  <!-- 
    transactionManager : JDBC와 MANAGED 둘중 하나의 타입을 설정할 수 있음
    - JDBC :트랜잭션을 내가 직접 관리(수동 COMMIT)
    - MANAGED :트랜잭션에 대해서 어떠한 영향도 행사하지 않겠다(자동 COMMIT)
  -->
    <transactionManager type="JDBC"/>
      
    <!-- 
      	dataSource :pooled와 unpooled 둘중 하나의 타입을 선택할 수 있음(connectionPool사용)
      	connectionPool : Connection객체를 담아둘 수 있는 영역
      						한번 생성된 Connectiond객체를 담아두면 재활용할 수 있다.
    -->
      <dataSource type="POOLED">
        <property name="driver" value="oracle.jdbc.driver.OracleDriver"/>
        <property name="url" value="jdbc:oracle:thin:@localhost:1521:xe"/>
        <property name="username" value="C##MYBATIS"/>
        <property name="password" value="MYBATIS"/>
      </dataSource>
    </environment>
  </environments>
  
  <!-- mapper : 실행할 sql문들을 기록해둔 mapper 파일을 등록하는 영역  -->
  <mappers>
    <mapper resource="/mappers/member-mapper.xml"/>
    <mapper resource="/mappers/board-mapper.xml"/>
    <mapper resource="/mappers/reply-mapper.xml"/>
  </mappers>
</configuration>